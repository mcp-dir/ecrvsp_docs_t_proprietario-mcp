# Ferramentas

ECRVSP Documentos: Transferência de Proprietário expõe 1 ferramenta (todas somente leitura).

### 1. `ecrvsp_docs_t_proprietario_consultar`
**Input**: `a3`, `a3_pin`, `login_cpf`, `login_senha`, `chassi`, `file_pdf` (opcional), `categoria`, `tipo_restricao`, `cpf` (opcional), `cnpj` (opcional), `rg` (opcional), `rg_orgao_expedidor` (opcional), `rg_uf` (opcional), `numero_espelho`, `endereco_cep`, `endereco_numero`, `endereco_complemento` (opcional), `nome`, `telefone` (opcional), `venda_data`, `venda_valor`, `veiculo_empresa_salvados`, `veiculo_seguradora`, `tipo_placa_dianteira`, `tipo_placa_traseira`, `tipo_placa_traseira_segunda`

ECRVSP Documentos: Transferência de Proprietário, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar ECRVSP Documentos: Transferência de Proprietário
```
